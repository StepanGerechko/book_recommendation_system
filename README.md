# Рекомендательная Система Книг (Book Recommendation System)

Первый pet-проект рекомендательной системы книг на основе датасета **Book-Crossing**.

Проект реализован в формате Jupyter Notebook и показывает полный базовый пайплайн рекомендательной системы:
- загрузка и очистка данных;
- фильтрация пользователей и книг;
- построение матрицы `user-item`;
- расчёт похожести пользователей;
- генерация персональных рекомендаций по введённым книгам и оценкам.

## О Проекте (About The Project)

В проекте используется **коллаборативная фильтрация на основе пользователей (user-based collaborative filtering)**: система ищет пользователей с похожими предпочтениями, а затем рекомендует книги, которые эти пользователи оценили высоко.

В ноутбуке реализованы три метрики сходства:
- **сходство Жаккара (Jaccard similarity)**;
- **евклидово сходство (Euclidean similarity)**;
- **сходство по читательскому профилю (Reading profile similarity)**.

Итоговая близость пользователей считается как взвешенная комбинация этих метрик.

## Возможности (Features)

- Работа с реальным датасетом книг и пользовательских оценок
- Фильтрация неявных оценок (`0`)
- Ограничение на минимальную популярность книг и активность пользователей
- Нормализация оценок для расчёта расстояния
- Поддержка пользовательского ввода в формате:

```python
my_ratings = {
    "Harry Potter": 9,
    "The Great Gatsby": 7,
    "The Hobbit": 8
}
```

- Вывод списка рекомендуемых книг на основе похожих пользователей

## Стек Технологий (Tech Stack)

- Python
- Pandas
- NumPy
- Jupyter Notebook

## Структура Проекта (Project Structure)

```text
.
├── book_recommendation_system.ipynb   # основной notebook проекта
├── datasets/
│   ├── Books.csv                      # информация о книгах
│   ├── Ratings.csv                    # пользовательские оценки
│   └── Users.csv                      # информация о пользователях
└── README.md
```

## Как Это Работает (How It Works)

### 1. Загрузка Данных (Data Loading)

Загружаются данные о книгах и оценках:
- `datasets/Books.csv`
- `datasets/Ratings.csv`

### 2. Предобработка Данных (Preprocessing)

- удаляются неявные оценки (`Book-Rating = 0`);
- данные объединяются по `ISBN`;
- остаются только книги с достаточным числом оценок;
- остаются только достаточно активные пользователи.

### 3. Матрица Пользователь-Книга (User-Book Matrix)

Строится матрица вида:
- строки: пользователи;
- столбцы: книги;
- значения: оценки.

### 4. Расчёт Сходства (Similarity Calculation)

Для нового пользователя система:
- находит введённые книги в датасете;
- формирует его вектор оценок;
- ищет похожих пользователей;
- считает итоговую похожесть через комбинацию нескольких метрик.

### 5. Генерация Рекомендаций (Recommendation Generation)

Из книг похожих пользователей выбираются те, которые:
- пользователь ещё не вводил;
- были высоко оценены похожими читателями.

После этого книги сортируются по итоговому `score` и возвращаются как рекомендации.

## Запуск Проекта (Running The Project)

### 1. Установка Зависимостей (Install Dependencies)

```bash
pip install pandas numpy jupyter
```

### 2. Запуск Jupyter Notebook

```bash
jupyter notebook
```

Открой файл `book_recommendation_system.ipynb` и выполни ячейки по порядку.

## Пример Ввода (Example Input)

```python
my_ratings = {
    "Harry Potter": 9,
    "The Great Gatsby": 7,
    "The Hobbit": 8
}

get_recommendations(my_ratings)
```

## Важное Примечание (Important Note)

Поиск книг в текущей версии работает по частичному совпадению строки без учёта регистра.

Это значит:
- `harry potter` и `Harry Potter` будут найдены одинаково;
- можно вводить часть названия;
- слишком короткий или неоднозначный ввод может привести не к той книге;
- если книга не попала в отфильтрованный набор данных, система её не найдёт.

## Ограничения (Limitations)

- Проект реализован в одном notebook без разбиения на модули
- Нет отдельного интерфейса или веб-приложения
- Нет автоматических тестов
- Текущий поиск по названию можно улучшить с помощью нечёткого поиска (fuzzy matching)
- Качество рекомендаций зависит от выбранной фильтрации и параметров метрик

## Возможные Улучшения (Future Improvements)

- Добавить нечёткий поиск по названиям книг
- Вынести логику из notebook в `.py` модули
- Добавить визуализацию данных
- Сравнить user-based и item-based подходы
- Сделать Streamlit или Flask интерфейс
